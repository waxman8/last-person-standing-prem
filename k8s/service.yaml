apiVersion: v1
kind: Service
metadata:
  name: last-person-standing-service
spec:
  type: LoadBalancer
  selector:
    app: last-person-standing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000